{% extends 'base.html.twig' %}

{% block title %}
    Modification de la catégorie: 
    {{ categorie.titre }}
    {{ parent() }}
{% endblock %}

{% block body %}
    <section class="container mt-4">
        <h1 class="text-center"> Modification de la catégorie {{ categorie.titre }} </h1>
        {% include 'Backend/Categorie/_form.html.twig' with {button_label: 'MODIFIER'} %}
        <a href="{{ path('admin.categories.index') }}" class="btn btn-primary mt-4"> Retour à la liste </a>
    </section>
{% endblock %}