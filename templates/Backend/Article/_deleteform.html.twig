<form action="{{ path('admin.articles.delete', {id: article.id}) }}" method="POST" onsubmit="return confirm('T sûr ?')">
    <input type="hidden" name="token" value="{{ csrf_token('delete' ~ article.id) }}">
    <button type="submit" class="btn btn-danger text-light">Supprimer</button>
</form>