{% extends 'base.html.twig' %}

{% block title %}
    Modification de l'article: 
    {{ article.titre }}
    {{ parent() }}
{% endblock %}

{% block body %}
    <section class="container mt-4">
        <h1 class="text-center"> Modification de l'article {{ article.titre }} </h1>
        {% include 'Backend/Article/_form.html.twig' with {button_label: 'MODIFIER'} %}
        <a href="{{ path('admin.articles.index') }}" class="btn btn-primary mt-4"> Retour à la liste </a>
    </section>
{% endblock %}